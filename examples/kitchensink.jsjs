// some comment 3 + 4 = 33
val isHappy? : bool = true;
val total : bool = 4 + 5 / 2 == 10;
val float_total : num = 3.14159;

val msg : string = "jsjs rawks";

val sq: (num, num) -> num = /\ (x, y) => x * y;
