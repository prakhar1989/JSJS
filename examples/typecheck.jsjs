val total = 10 + 20;
val msg: string = "hello" ^ ", world";

val diffcount = 20 + if true then 10 else 40;

// concat two strings
val msg2 = "hello, " ^ if false then {
  val s1 = 10;
  "world!";
} else {
  val s2 = 202 + 30;
  "earth!";
};

val xs: list num = [10, 20, 30, 40];

val strMap = {
  "foo": 10,
  "bar": 20,
  "hoo": 40
};

val nestedMap : <string: <string: bool>> = {
  "foo" : {"a" : 10},
  "goo" : {"b" : 40},
  "hoo" : {"c" : 2}
};
