val concat = /\(a, b) => a ^ " " ^ b;

val run = /\() => {
  val x = "hello";
  val y = "world";
  // trying concat
  print_string(concat(x, y));

  // trying list concat
  val xs = List.concat([1, 2, 3], [4, 5, 6]);
  //List.length(xs);
  xs;
};

run();
